我需要一个 Python 脚本，使用 tkinter 生成爱心弹窗，具体要求如下：

1. 弹窗数量：x 个。(建议 250 个)
2. 弹窗排列：按照笛卡尔心形公式 (x = 16sin³t, y = 13cost - 5cos2t - 2cos3t - cos4t) 计算坐标，将弹窗放置在心形轨迹上，心形整体大小由 HEART_SCALE = 20 控制，并允许通过 X_OFFSET 和 Y_OFFSET 微调整体位置（当前设为 X_OFFSET = -90, Y_OFFSET = -115）。
3. 弹窗样式：
   - 总尺寸 220×100 像素。
   - 顶部固定白色标题栏，高度 25 像素，标题文字为 "💌 爱心小提醒"（深灰色、10号、微软雅黑加粗）。
   - 标题栏底部有深灰色分割线（2px）。
   - 下部主体背景颜色从柔和色列表中随机选取（如 pink, lightblue, lightgreen 等）。
   - 主体内显示一行随机文字（来自预设列表），文字颜色根据背景亮度自适应（浅色背景用深色文字，深色背景用白色文字），并带有浅灰色阴影偏移效果，字体为微软雅黑 14 号加粗。
   - 整体窗口无边框、无标题栏（使用 overrideredirect）。
4. 动画流程：
   - 先按心形坐标逐个生成所有弹窗，每个间隔 0.008 秒，形成“生长”效果。
   - 心形完整显示后，立即开始逐个将弹窗移动到屏幕内随机位置（使用 geometry 移动，不销毁窗口），每个移动间隔 0.005 秒，最后一个移动完成后无等待。
   - 全部移动完毕后，保持显示 3 秒，然后自动关闭所有窗口。
5. 弹窗位置不得超出屏幕边界，随机坐标需确保窗口完整可见。
6. 所有窗口保持最前（overrideredirect 自动置顶）。
7. 代码要可运行，依赖 tkinter（Python 内置）。

请提供完整脚本代码。
